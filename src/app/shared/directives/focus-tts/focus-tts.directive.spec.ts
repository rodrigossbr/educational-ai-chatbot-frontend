import { FocusTtsDirective } from './focus-tts.directive';

describe('FocusTts', () => {
  it('should create an instance', () => {
    const directive = new FocusTtsDirective();
    expect(directive).toBeTruthy();
  });
});
